on:
  pull_request: {}
  push:
    branches:
    - main
    - master
name: Semgrep
jobs:
  semgrep:
    name: Scan
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: returntocorp/semgrep-action@v1
      with:
        config: >- # more at semgrep.dev/explore
          p/security-audit
          p/secrets
        #auditOn: push
        #publishToken: ${{ secrets.SEMGREP_APP_TOKEN }}
        #publishDeployment: 2078
    - run: |
         cat sarif_file.json

